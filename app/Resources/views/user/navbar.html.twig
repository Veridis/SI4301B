<!--<ul class="nav nav-tabs">
    <li {% if app.request.attributes.get('_route') == 'site_home' %}class="active"{% endif %}><a href="{{ path('site_home') }}">Home</a></li>
    {% if  app.user %}
        <li {% if app.request.attributes.get('_route') == 'user_show' %}class="active"{% endif %}><a href="{{ path('user_show', { 'id' : app.user.id }) }}">Profile</a></li>
        <li {% if app.request.attributes.get('_route') == 'user_logout' %}class="active"{% endif %}><a href="{{ path('user_logout') }}">logout</a></li>
    {% endif %}
</ul>-->

<nav class="navbar navbar-inverse" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('site_home') }}">Home</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('musics') }}">Musics</a></li>
            </ul>
            <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="artist, title, ...">
                </div>
                <button type="submit" class="btn btn-default">Search</button>
            </form>
            <form class="navbar-form navbar-left" role="mp3">
                <div class="mp3">
                    <audio controls>
                        <source src="{{ asset('uploads/musics/songs') }}"> </source>
                        Veuillez mettre à jour votre navigateur</audio>
                    </audio>
                </div>
            </form>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><a href="{{ path('mymusics') }}">My musics</a></li>
                    <li {% if app.request.attributes.get('_route') == 'user_show' %}class="active"{% endif %}><a href="{{ path('user_show', { 'id' : app.user.id }) }}">{{ app.user.username }}</a></li>
                    <li><a href="{{ path('user_logout') }}">Logout</a></li>
                {% else %}
                    <li {% if app.request.attributes.get('_route') == 'user_login' %}class="active"{% endif %}><a href="{{ path('user_login') }}">Login</a></li>
                    <li {% if app.request.attributes.get('_route') == 'user_new' %}class="active"{% endif %}><a href="{{ path('user_new') }}">Register</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
