{% extends 'AMUserBundle::layout.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <div class="well">
                    <form class ="form-horizontal" action="{{ path('user_create') }}" method="post">
                        <h2>Register </h2>
                        <div class="form-group">
                            {% if form_errors(create_form.username)|length > 0 %}
                            <div class="alert alert-danger alert-dismissable">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                                {{ form_errors(create_form.username) }}
                            </div>
                            {% endif %}
                            <span class="col-sm-2 control-label">{{ form_label(create_form.username) }}</span>
                            <div class="col-md-10">
                                {{ form_widget(create_form.username) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {% if form_errors(create_form.plainPassword.first)|length > 0 %}
                            <div class="alert alert-danger alert-dismissable">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                                {{ form_errors(create_form.plainPassword.first) }}
                            </div>
                            {% endif %}
                            <span class="col-sm-2 control-label">{{ form_label(create_form.plainPassword.first) }}</span>
                            <div class="col-md-10">
                                {{ form_widget(create_form.plainPassword.first) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {% if form_errors(create_form.plainPassword.second)|length > 0 %}
                            <div class="alert alert-danger alert-dismissable">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                                {{ form_errors(create_form.plainPassword.second) }}
                            </div>
                            {% endif %}
                            <span class="col-sm-2 control-label">{{ form_label(create_form.plainPassword.second) }}</span>
                            <div class="col-md-10">
                                {{ form_widget(create_form.plainPassword.second) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {% if form_errors(create_form.email)|length > 0 %}
                            <div class="alert alert-danger alert-dismissable">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                                {{ form_errors(create_form.email) }}
                            </div>
                            {% endif %}
                            <span class="col-sm-2 control-label">{{ form_label(create_form.email) }}</span>
                            <div class="col-md-10">
                                {{ form_widget(create_form.email) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-12">
                                <input class="btn btn-lg btn-info btn-block" type="submit" value="Register" />
                            </div>
                        </div>
                        {{ form_rest(create_form) }}
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}